import state from "./getters.js"
const getters = {
    // money(state){
    //     let money = 0
    //     state.car.forEach((item) => {
    //         money+=(item.price*item.num)            
    //     });
    //     return money
    // }
}
export default getters